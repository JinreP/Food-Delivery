export type AddProps = {
  text: string;
};

export type ArrayPropsTypes = {
  title: string;
  price: number;
  length: number;
};

export type CategoryTypes = {
  category: string;
};

export type FoodsTypes = {
  food: string;
  price: number;
  image: string;
  ingredients: string;
};
